<p id="notice"><%= notice %></p>
<table border="2">

  <tr>
    <th><strong>First name:</strong></th>
    <td><%= @customer.first_name %></td>
  </tr>

  <tr>
    <th><strong>Last name:</strong></th>
    <td><%= @customer.last_name %></td>
  </tr>

  <tr>
    <th><strong>Phone number:</strong></th>
    <td><%= @customer.phone_number %></td>
  </tr>

</table><br><br>

<%= link_to 'Edit', edit_customer_path(@customer), class: "btn btn-primary"%> |
<%#= link_to 'Back', root_path %>
<%= link_to 'Booking', customer_bookings_path(customer_id: @customer.id), class: "btn btn-primary" %> |

<%# if session[:customer_id] %>
  <%= link_to 'Logout', logout_path, :method => :delete, class: "btn btn-primary"	%>
<%# end %>
